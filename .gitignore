# Dependencies
node_modules
dist
dist-ssr

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Downloads now come from GitHub Releases, not a local copy
website/*.exe

# Secrets — NEVER commit signing keys
.tauri-keys/
.toolchain/
tools/license-private.pem

# Claude Code session state
.claude/

# Local env (Supabase URL/key) — use .env.example as the template
.env
.env.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.log
*.local

# Local test build (CDP-driven testing)
src-tauri/target-test
